学习了C++的语法

语句是C++中的一种多分支选择结构，用于根据不同的条件执行不同的代码块。其基本语法如下：
根据括号中的内容选择不同的代码块，每个case语句后跟一个标签值，标签值必须是常量表达式。
switch()
{
    case"1";
    xxx
    break;
    case"2";
    xxx
    break;
}

用c++生成exe文件后要在.cpp文件中加入
    cout << "请按任意键结束...";
    cin >> a;
不然会出现运行结果一闪而过的问题
  